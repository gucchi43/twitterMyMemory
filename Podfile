# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'twitterMyMemory' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'TwitterCore'
  pod 'TwitterKit'
  pod 'SwiftyJSON'
  pod 'Alamofire'
  pod 'SVProgressHUD'  
  pod 'SwiftDate'

  target 'twitterMyMemoryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'twitterMyMemoryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
